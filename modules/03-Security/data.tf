# Retrieve VPC ID from the SSM Parameter Store
# This data source is used to fetch the VPC ID from the SSM Parameter Store.
# The VPC ID is stored in the SSM Parameter Store by the Networking module.
